interface:
  display_name: "Compicker"
  short_description: "Compare UI options in app or design lab"
  default_prompt: "Use $compicker to compare these visual UI options with a temporary picker or design-lab route, then help lock in the winner."
